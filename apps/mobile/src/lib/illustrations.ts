/**
 * Typed registry of bundled illustration assets. Each is generated by the
 * OpenAI image pipeline (`scripts/generate-assets.ts`) and copied into
 * `apps/mobile/assets/illustrations/`. One import site so screens never reach
 * into asset paths directly. Imported (not required) so `expo/types` provides
 * the asset module typing.
 */
import welcomePeople from '../../assets/illustrations/welcome-people.png';

export const illustrations = {
  /** First-run welcome hero — four friends + floating interest badges. */
  welcomePeople,
} as const;

export type IllustrationKey = keyof typeof illustrations;
